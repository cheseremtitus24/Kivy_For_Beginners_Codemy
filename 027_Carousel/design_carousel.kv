<MyLayout>
    BoxLayout:
        orientation: 'vertical'
        size: root.width, root.height

        Carousel:
            direction; 'right' #left, top, bottom

            Image:
                source: 'image/path'

            AsyncImage: #internet sourced image
                source: 'url/to/image'

